/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Next 15 App Router is default; enable optional features as needed
    typedRoutes: true
  },
  reactStrictMode: true
};

export default nextConfig;
